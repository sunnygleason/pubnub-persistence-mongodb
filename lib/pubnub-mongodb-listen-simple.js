// Generated by CoffeeScript 1.7.1
(function() {
  var CHANNEL, PUBNUB, pubnub, _;

  PUBNUB = require('../deps/pubnub-javascript/node.js/pubnub.js');

  _ = require('../deps/underscore/underscore.js');

  CHANNEL = process.argv[2];

  pubnub = PUBNUB.init({
    subscribe_key: 'demo',
    publish_key: 'demo'
  });

  pubnub.subscribe({
    channel: CHANNEL,
    message: function(command) {
      return console.log("got from \#" + CHANNEL + ":", JSON.stringify(command));
    }
  });

  console.log("Listening to PubNub MongoDB channel \#" + CHANNEL);

}).call(this);
